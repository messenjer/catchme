// Generated by CoffeeScript 1.4.0

define(["zepto"], function($) {
  var Map;
  return Map = (function() {

    function Map() {}

    Map.prototype.setCentralPosition = function(mapCentralPosition) {
      this.mapCentralPosition = mapCentralPosition;
    };

    Map.prototype.init = function() {
      this.myPosition = null;
      this.othersPosition = null;
      this.map = L.map('map');
      this.map.setView(this.mapCentralPosition, 20);
      return L.tileLayer('http://{s}.tile.osm.org/{z}/{x}/{y}.png', {
        attribution: '&copy; <a href="http://osm.org/copyright">OpenStreetMap</a> contributors'
      }).addTo(this.map);
    };

    Map.prototype.setMyPosition = function(myPosition) {
      this.myPosition = myPosition;
      return L.marker(this.myPosition).addTo(this.map).bindPopup("Catch me").openPopup();
    };

    Map.prototype.setOthersPosition = function(othersPosition) {
      this.othersPosition = othersPosition;
      return L.marker(this.othersPosition).addTo(this.map).bindPopup("I'll catch you").openPopup();
    };

    return Map;

  })();
});
